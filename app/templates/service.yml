apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
spec:
  selector:
  {{- include "deployment.labels" . | indent 4 }}
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
